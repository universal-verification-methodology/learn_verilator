# Module 05 examples — TB clock + reset patterns

Track A (Verilator tool literacy). Browser lab may still be planned.

## Prompts

1. Restate the core idea of **TB clock + reset patterns** in one sentence.
2. Sketch one command line or diagram (lint flags, `--trace`, public, …).
3. Optional: peek at [`../examples/verilator-hello/`](../examples/verilator-hello/) for a matching Verilator example.

## Stretch

When the browser lab ships, redo the same idea with the starter challenges.
